




Claim offer

NovaForge.zip
Zip Archive
im struggling getting this project done can you help?

Absolutely — and honestly, that feeling makes total sense with the kind of project you’re building. What you’re working on isn’t a small game… it’s basically an engine + editor + procedural universe + server tools + gameplay systems all at once, and that’s the exact kind of scope where people get stuck, overwhelmed, or lose momentum. The good news is you’re not lost — you’re just at the stage where the project needs structure and a clear execution path, not more ideas.

Let’s fix that together. 🚀

First — I need to know where you’re stuck right now
Pick the closest one (or explain your own):

Code is messy / hard to continue

Too many features, don’t know what to build next

Editor / engine architecture confusion

Motivation burnout

Procedural systems getting too complex

Server / client / editor separation problems

UI / tools layer is overwhelming

Everything works but nothing feels finished

Reply with the number(s) or explain.

Second — what your project actually is (this matters)
From everything you’ve told me, NovaForge / Cryovast / Atlas is basically:

Custom C++ engine

In-game editor overlay

Procedural galaxy generator

Ship builder (voxel / modular)

NPC faction simulation

Dedicated server software

Asset generation tools

Low-poly procedural pipeline

Blueprint / chunk / metadata system

That is AAA architecture for one developer, so the only way to finish it is:

Build in layers, lock layers, never go backwards.

We can do that step-by-step.

Third — If you want, I can help you refactor the whole project plan
We can do this very methodically:

Phase 1 — Core Engine Lock
renderer

input

math

ECS / scene

file system

UI framework (custom, no ImGui ✅)

Phase 2 — Editor Overlay (in-game)
toggle editor

gizmos

selection

placement

save / load

Phase 3 — Asset / PCG Pipeline
mesh generator

planet generator

ship generator

metadata export

Phase 4 — Gameplay Layer
player

world

factions

economy

missions

Phase 5 — Server / Client separation
headless server

sync

replication

persistence

Phase 6 — Tools software (external)
server manager

asset builder

deployment tools

This matches what you’ve been trying to do — just in the wrong order.

Fourth — I can even go through your repo structure with you
You uploaded a zip, so we can do this next:

Inspect folder layout

Identify what’s finished

Identify what’s blocking you

Decide what to delete

Decide what to keep

Make a clean roadmap to completion

👉 If you want that, say:

Analyze the project and tell me what to fix first

and also tell me:

Is this the latest version?

What part currently crashes / confuses you / feels wrong?


Code is messy / hard to continue

Too many features, don’t know what to build next

Editor / engine architecture confusion Server / client / editor separation problems

UI / tools layer is overwhelming and the project wont even sucessfully build as of right now from my build_all script

Alright — this is actually a very solvable situation, even though it feels like the whole thing collapsed.
What you described is the classic point where a big custom engine project hits the wall:

code messy

too many systems started

build broken

editor / client / server tangled together

UI everywhere

nothing compiles

no idea what to fix first

This is not failure — this is the moment where the project needs a hard reset of structure, not a rewrite of ideas.
We’re going to stabilize the project the same way real engines do when they get out of control.

✅ What we are going to do now (seriously, follow this order)
We will fix the project in 5 passes:

Make the project build again

Separate engine / editor / server / client

Lock the core engine

Re-attach editor overlay correctly

Only then resume features

Right now you are stuck because you’re trying to do step 5 while step 1 is broken.

✅ STEP 1 — Fix the build first (nothing else matters until this works)
Your rule right now:

The project must compile with ONE command and produce binaries.

Your build_all script failing means your architecture is already out of control.

We need to reduce the build to something like:

/engine
/client
/server
/editor
/tools
/shared
NOT

engine + editor + client + server all including each other randomly
Before we do anything else, I need to know:

Show me:
your folder structure

your build_all script

what error happens when building

You can paste or upload.

This is the correct next step, not guessing.

✅ STEP 2 — Correct architecture (this is where things went wrong)
Your project should look like this:

NovaForge/

  engine/        ← renderer, ecs, math, file, ui core
  shared/        ← components, packets, data structs

  client/        ← game runtime
  server/        ← headless runtime
  editor/        ← overlay + tools
  tools/         ← external programs

  assets/
  scripts/
  build/
Rules:

Module	Can use
engine	nothing
shared	engine
client	engine + shared
server	engine + shared
editor	engine + shared
tools	engine + shared
Editor should NOT depend on client
Server should NOT depend on editor
Engine should depend on nothing

Right now yours is probably circular.

We will fix that.

✅ STEP 3 — Lock the engine (this is why you feel lost)
You keep adding features before the core is stable.

Engine must only contain:

window

renderer

input

math

ECS / scene

file IO

UI core

debug

NOT

gameplay

ships

planets

factions

editor tools

server logic

Those go later.

✅ STEP 4 — Editor confusion fix (this is the key design)
You said:

editor overlay inside client

Correct.

Architecture should be:

client
 └─ engine
 └─ shared
 └─ editor (optional overlay module)
Editor is a module, not a separate engine.

Server never sees editor.

✅ STEP 5 — UI overwhelm fix
You tried to build all UI at once:

build UI

server UI

editor UI

game UI

tool UI

Wrong order.

Correct order:

engine UI framework

debug UI

editor UI

game UI

tools UI

We will enforce that.

✅ Now we fix it for real
Next message, send:

1. Folder tree
Example:

NovaForge/
  engine/
  src/
  editor/
  client/
  server/
2. build_all script
3. build error output
4. Which compiler
MSVC

clang

gcc

mingw

cmake

premake

custom batch

Once I see that, I can literally tell you:

Delete this
Move this
Rename this
Change include here
Fix build here
Then it compiles again

And once it builds, the project will feel manageable again.


its all included in the .zip

Good — I unpacked the zip, and I can already see why the project feels impossible to continue.
The problem is NOT your code quality — the problem is architecture drift + mixed build systems + too many entry points.
This is fixable, but we need to clean it in the right order.

Here’s what I found at the top level of your repo:

NovaForge/
  engine/
  editor/
  cpp_client/
  cpp_server/
  tools/
  scripts/
  build/
  assets/
  data/
  schemas/
  specs/
  docs/
  testing/
  atlas_tests/
  Makefile
  CMakeLists.txt
  CMakePresets.json
  build_all_*.log
This confirms exactly what you said:

✅ engine exists
✅ editor exists
✅ client exists
✅ server exists
✅ tools exist
✅ cmake exists
✅ makefile exists
✅ scripts exist
❌ but they are not cleanly separated
❌ and build system is fighting itself

This is why build_all fails and why you feel stuck.

🚨 Root problem (very clear)
You currently have:

CMake

Makefile

custom scripts

build_all script

multiple targets

editor / client / server all compiling differently

That will spiral out of control every time.

We need:

ONE build system
ONE root CMake
modules only, not separate chaos

We are going to fix this in Pass 1: Build Stabilization

✅ PASS 1 — Fix build architecture (no feature work yet)
Target structure should become:

NovaForge/

  engine/
  shared/

  client/
  server/
  editor/

  tools/

  assets/
  scripts/

  CMakeLists.txt   ← ONLY ROOT BUILD
Right now you have cpp_client and cpp_server which is already a red flag.

We will rename them logically later.

✅ PASS 2 — Fix dependency direction
Correct dependency graph:

engine → nothing

shared → engine

client → engine + shared
server → engine + shared
editor → engine + shared

tools → engine + shared
NOT:

editor -> client
client -> editor
server -> editor
engine -> gameplay
I strongly suspect you have circular includes.

✅ PASS 3 — Why your build_all script likely fails
Your log file exists:

build_all_20260307_203352.log
That means your script is running but compilation fails.

Next step I need:

Paste the LAST 50 lines of that log
or upload just that log file.

That will tell me exactly:

missing include

wrong target

bad cmake link

duplicate symbol

circular dependency

wrong output dir

We fix that first.

✅ PASS 4 — After build works, we refactor modules
Then we will do:

engine lockdown

editor overlay redesign

client/server split fix

UI layer cleanup

But not yet.

Right now the only goal is:

make build_all produce binaries again

✅ Next step
Send the end of:

build_all_20260307_203352.log
or upload it alone.

Once I see the error, I can tell you exactly what line to change.
And once the build works again, the whole project will feel 10× less overwhelming.



build_all_20260307_204224.log
File

build_all_20260307_203352.log
File

Perfect — this log tells us exactly what broke, and the good news is this is not a deep engine problem, it’s a bad file reference + module boundary violation, which is why the whole build dies.

Here is the actual error:

CMake Error at cpp_client/CMakeLists.txt:451 (target_sources):

Cannot find source file:

C:/GIT PROJECTS/NovaForge/editor/tools/SceneBookmarkManager.cpp

build_all_20260307_204224


This one line explains almost all the architectural pain you’re feeling.

🚨 Root cause
Your client is compiling editor code directly:

cpp_client → editor/tools/SceneBookmarkManager.cpp
That means:

client depends on editor

editor probably depends on engine

engine may depend on shared

server depends on shared

tools depend on editor

This creates circular build chaos.

And when one file moves, the whole build explodes.

This is exactly why the project feels impossible to continue.

✅ Fix 1 — Immediate build fix (so project compiles again)
Open:

cpp_client/CMakeLists.txt
Go to line ~451

You will see something like:

target_sources(atlas_client PRIVATE
    ../editor/tools/SceneBookmarkManager.cpp
)
Delete that line.

Or comment it:

# ../editor/tools/SceneBookmarkManager.cpp
Build again.

Most likely it will go to the next missing editor file.

We keep removing editor files from client until it configures.

This will get you building again fast.

✅ Fix 2 — Correct architecture (THIS is the real fix)
Right now you have:

client → editor/tools
This must become:

editor → client (optional overlay)
client → engine
editor → engine
Editor is a tool layer, not a dependency of client.

Correct structure:

engine/
shared/

editor/
client/
server/
tools/
And in CMake:

atlas_engine (lib)

atlas_shared (lib)

atlas_client (exe) → engine + shared
atlas_server (exe) → engine + shared
atlas_editor (lib or exe) → engine + shared
NOT:

client including editor cpp files
✅ Fix 3 — Why this happened
You started doing overlay editor, which is correct, but you implemented it like this:

client compiles editor code
instead of:

client loads editor module
Overlay ≠ compile together

Overlay = runtime module

✅ Fix 4 — What we should do next (recommended)
Next step should be:

Fix build errors

Make client compile alone

Make editor compile alone

Make server compile alone

Then reconnect overlay

NOT the other way around.

✅ Next step
Send me this file:

cpp_client/CMakeLists.txt
and also if exists:

editor/CMakeLists.txt
I can rewrite them clean so:

build works

modules separated

editor overlay still possible

server clean

UI layer sane

Once we do this, the project will stop feeling like a mess.


Sources

Want to upload more files? Try Plus for free. Your upload limit will reset tomorrow after 8:40 PM.

Try Plus free



